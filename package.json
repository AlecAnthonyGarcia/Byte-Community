{
	"name": "byte-community-server",
	"version": "0.1.0",
	"main": "server/index.js",
	"dependencies": {
		"axios": "^0.19.0",
		"express": "^4.17.1",
		"moment": "^2.24.0"
	},
	"scripts": {
		"start": "NODE_ENV=production node server/index.js",
		"heroku-postbuild": "cd client && npm install && npm run build"
	},
	"engines": {
		"node": "^8.10.0"
	}
}
